import 'package:flutter/material.dart';

class ImmersiveEditor extends StatelessWidget {
  const ImmersiveEditor({super.key, required controller, required focusNode});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Editor UI placeholder'),
    );
  }
}
